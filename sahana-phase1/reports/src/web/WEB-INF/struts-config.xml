<?xml version="1.0" encoding="ISO-8859-1" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.1//EN"
          "http://jakarta.apache.org/struts/dtds/struts-config_1_1.dtd">
<!--
     This is the Struts configuration file for the example application,
     using the proposed new syntax.

     NOTE:  You would only flesh out the details in the "form-bean"
     declarations if you had a generator tool that used them to create
     the corresponding Java classes for you.  Otherwise, you would
     need only the "form-bean" element itself, with the corresponding
     "name" and "type" attributes.
-->


<struts-config>

    <!--form-beans>
        <form-bean name="LEFMForm" type="com.virtusa.aeceg.ved.struts.form.LastEngineeringFocusMetricsForm"/>
    </form-beans -->

    <global-forwards>
        <forward name="failure" path="/error.html"/>
    </global-forwards>
    
    <!-- ========== Action Mapping Definitions ============================== -->
    <action-mappings>
         <!-- Action Mappings  for last-eng-focus-metrics page -->
        <action path="/sahana-report" input="" parameter="" type="gov.lk.dms.report.action.erms.SahanaReportAction" scope="request" validate="false">
            <forward name="success" path="/reports/report.jsp" redirect="false"/>
            <forward name="failure" path="/error-page.html"/>
        </action>

         <!-- Action Mappings  for last-eng-focus-metrics page -->
        <action path="/flush-reports" input="" parameter="" type="gov.lk.dms.report.common.FlushCacheAction" scope="request" validate="false">
            <forward name="success" path="/reports/flush.jsp" redirect="false"/>
            <forward name="failure" path="/error-page.html"/>
        </action>

    </action-mappings>

  
  <!-- ========== Tiles plug-in setting settings =================== -->
  <!-- Here we specified the tiles plug-in.
       This plug-in register appropriate Request Processor -->
  <!--  <controller processorClass="org.apache.struts.tiles.TilesRequestProcessor"  />  -->

  <!-- ========== Associated Messages Ressource settings =================== -->
  <!-- Not used by tiles or this website, but needed due to a bug in actual Struts version -->
  <message-resources parameter="org.apache.struts.webapp.tiles.dev1-1.ApplicationResources" null="false" />

   
</struts-config>
